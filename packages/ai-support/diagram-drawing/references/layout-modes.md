# 两种模式

faithful（默认）：保留截图中的相对位置、分组、方向、文字和主要路径，样式在支持范围内近似。按截图尺寸估算坐标，先确定容器和整体尺度，再填写组内相对坐标。不对拥挤区域擅自重新排版。

relayout：语义相同，允许统一样式、间距和路径。流程图采用稳定的主方向，分支标签放在对应连线附近；架构图保留服务归属与连接方向，尽量减少穿过节点的线。不凭空增加说明或删掉冗余但存在的关系。

局部改稿：用户没有要求移动的对象应保留坐标；不为美化全图改变既有手工布局。只有用户要求整体重排时才能改变无关对象位置。
