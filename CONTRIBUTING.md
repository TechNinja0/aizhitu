# 贡献指南

欢迎改进 AI智图的使用体验、文档、兼容性和可复现问题。

## 提交问题

通过本仓库 GitHub Issues 提交问题，请包含：系统、Node / 浏览器版本、复现步骤、预期与实际结果，以及最小示例。必要时附 `doctor --json` 的结果，先移除本机路径等个人信息。

不要上传账号凭据、客户截图或公司内部架构图。尽量使用自行构造的脱敏图稿。安全问题按 [SECURITY.md](SECURITY.md) 处理。

## 本地开发

```sh
npm ci
npm run setup
npm run dev
```

默认地址为 `http://127.0.0.1:4317`。停止开发服务后再启动同端口的生产服务。

## 提交改动

1. Fork 仓库，在自己的分支中修改。
2. 对行为变化补充必要测试；纯文档改动检查命令、链接和能力描述。
3. 更新相关使用说明；不要将未完成或未验证的能力写成已支持。
4. 提交 Pull Request，说明解决的问题、主要改动和实际验证结果。

基础检查：

```sh
npm run build
npm test
npm run docs:check
```

涉及编辑器交互、保存、导出或服务端逻辑时，再运行对应浏览器检查；准备发布时运行：

```sh
npm run check
```

`npm run check` 已包含基础交互、导出、美化、AI 会话、版本恢复及主题/预览回归。定向调试可运行 `npm run test:ai-chat`、`npm run test:product-audit` 或 `npm run test:product-polish`。CI 使用模拟 CLI，不需要账号或 API Key；真实客户端能力测试需手动运行，并记录模型与客户端版本。

## 回归约定

- 预览测试要验证弹窗可见、图片解码成功，以及失败可重试。
- 修改身份比较时同时测试字体规范化、视口变化、编辑后撤销、实际内容冲突和校验后并发修改。不得通过删除冲突保护来修复误报。
- 样式操作测试内容、几何与连线不变、锁定保护和一次撤销；检查不同窗口下的工具栏与主题卡片。

## 改动约定

- 自有逻辑优先写在 adapter 或独立模块中，不直接修改 `vendor/drawio` 的压缩文件。
- 升级上游资源时同时更新版本、来源、SHA-256 和第三方声明。
- 引入字体、图标、模板或依赖时核实分发条款，保留原始声明。
- 保持 `.drawio` 校验与防覆盖约定；不以静默丢弃未知内容的方式修复文件。
- 不提交 `node_modules`、构建目录、本地测试日志、个人图稿或 AI 会话记录。

真实 AI 基准脚本是维护者的手动实验工具，会调用外部客户端并消耗额度；部分脚本包含本机客户端路径和较宽权限配置。请先阅读源码、调整本地客户端与权限，再在仅含合成样本的目录中运行。普通贡献无需运行它们。

## 贡献许可

除非另有明确说明，提交供本项目接收的原创贡献适用项目的 Apache-2.0 许可证。提交者应有权提供这些内容，并注明引用的第三方来源及许可。参见 [LICENSE](LICENSE)、[NOTICE](NOTICE) 和 [第三方声明](THIRD_PARTY_NOTICES.md)。
