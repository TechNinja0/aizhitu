# 更新记录

## 未发布 · 多人同时编辑

- 新增主动加入的多人协同模式，与已有独占编辑互斥，支持在线成员和每秒自动同步。
- 按图形对象属性合并，加入删除优先、循环分组拒绝、幂等提交、断线重连、协同撤销和权限撤销保护。
- 文件库显示协同状态，协同期间阻止独占历史恢复和文件管理操作。
- 修复应用在隐藏工作目录中启动时固定编辑器资源和图标无法加载的问题。
- 新增 [技术方案](docs/design/COLLABORATION.md) 与 [自测报告](docs/testing/COLLABORATION_REPORT.md)。

## 1.6.0 · 2026-09-20

- 首次保存名称或内容校验失败时允许修正后重试；网络响应不明时保留提交并支持下载副本。

- 新建空白/模板不立即落库；首次修改内容或名称后自动保存，未修改返回不留下文件。支持主动保留原始模板与空白。
- 合并草稿与全部文件，历史草稿原 ID、内容、版本和回收站状态保留；保存默认私有，分享需单独授权。
- 首次保存增加按用户隔离的重复请求保护；网络响应丢失后可刷新重试，不重复生成文件。

## 1.5.0 · 2026-09-20

- 密码统一为至少 6 个字符，不限制字符组合；注册、原身份补设、重置与修改密码使用相同规则。

- 固定登录名与密码、记住登录、原浏览器身份补设；跨浏览器恢复同一文件归属与分享权限。
- 新增账号设置和本机用户管理：创建、一次性密码设置、停用/启用、全部图稿转移、受保护删除。
- 密码采用随机盐 scrypt；重置或停用撤销旧会话和编辑锁；管理操作校验版本并记录审计。
- 支持工作台与画布原生 HTTPS 证书参数，增加账号、迁移、多浏览器管理与 HTTPS 导出验证。


## 1.4.0 · 2026-09-20

- 保存文件与分享权限分离，新建及存入文件库均默认私有。
- 分享窗口支持仅自己、指定成员和所有成员；按浏览器身份 ID 授权，复制链接不自动开放。
- 文件列表、直链、历史版本、编辑与文档 AI 统一校验权限；收回授权立即清除失效编辑锁，在线页面轮询后移除画布。
- 分享权限版本独立于内容版本；本机管理员保留管理权。旧文件迁移为私有，内容、版本和链接保留，需要重新授权。
- 新增多身份接口与浏览器回归测试，更新使用、安全及架构文档。


## 1.3.0 · 2026-09-20

- 文件库操作列精简为分享和垃圾桶图标；点击文件名打开图稿，重命名通过编辑页顶部名称完成。列表分享复用链接窗口和复制失败后的手动复制方式。

- 新建、模板、导入默认创建服务器草稿，自动保存但不公开；确认名称后加入共享文件库，保持文档 ID。草稿内容、状态、历史和编辑接口按创建者/管理员隔离。
- 文件库增加草稿区、我创建的筛选、数量、列表重命名、单个/批量移入回收站与恢复。管理操作验证所有权、版本和编辑锁，批量失败时整批回滚。
- 发布命名期间继续续约，发布失败可重试；返回文件库前保存并释放编辑权。旧文件保持共享，保留内容、版本与创建人。
- 文件库明确显示本机管理员入口和权限范围，继续使用用户名加浏览器身份，不恢复密码或访问码。

## 1.2.1 · 2026-09-20

- 文件库新增独立创建人列，与最后保存人区分；创建人持久保存，旧文件按创建版本或创建者身份补全。

- 修复分享链接点击反馈不明显：立即显示分享窗口，复制失败可手动选中链接；剪贴板 API 被拒绝时尝试备用复制，分享不再等待文档同步。

- 工作区入口简化为首次填写用户名，同一浏览器自动记住身份；移除密码、访问码和退出入口，支持修改用户名而保留文件归属。
- 同名不会接管他人的身份；断网保留浏览器凭据，有效旧会话自动迁移。管理员入口限定服务电脑本机地址，旧远程管理员转为成员。
- 保留单人编辑锁、保存后自动同步、版本和回收站；补充身份持久化、旧会话迁移和多人浏览器回归验证。

## 1.2.0 · 2026-09-18

- Node.js 最低版本调整为 22.22，推荐 24 LTS，以匹配内置 SQLite 的已验证运行环境。
- 新增局域网共享文件库、个人账号与成员/管理员角色、独立文档 URL、重命名、搜索、回收站和分享链接。
- SQLite 持久保存文档和最近 50 个服务器版本；编辑锁绑定账号和标签页，30 秒租约、2 秒续约/自动保存/同步，版本冲突拒绝覆盖。
- 断线或锁失效后自动只读并保留本地修改，支持下载恢复副本、重新获取编辑权和加载最新版本。
- AI 任务按账号/文档归属，串行排队并可取消；仅管理员配置服务器客户端，导出结果按账号隔离。
- 新增多人 API 竞争测试与多浏览器模拟，补齐部署、备份、迁移和使用说明。保留 `/local` 临时画布。

## 1.1.0 · 2026-09-18

### 修复

- 画布改为普通滚轮滚动、Command+滚轮缩放；空格+鼠标左键拖动平移，不触发页面滚动，输入文字不受影响。

- AI 客户端设置采用固定底栏，保存按钮突出显示并标明未保存状态；连接测试单独分区，小窗口滚动时保存仍可见。

- 候选预览点击后立即打开独立大图窗口，展示加载、失败、重试状态；不再把结果藏在差异列表下方。
- 统一规范化后的内容摘要，忽略属性顺序、等价数值写法和样式顺序；解决字体规范化引发的候选误过期。
- 原稿编辑后撤销回相同内容可应用候选；使用新快照 revision 防止比较后发生的新编辑被覆盖。
- 候选可单独下载；预览失败或真实内容冲突不会迫使用户丢弃 AI 结果。

### 新增与完善

- 10 套主题、填充/边框/文字自定义颜色、十六进制输入、最近 10 个颜色快捷按钮。
- 复制/粘贴外观、6 种对齐和 2 种等间距排列；跳过锁定对象，保持图稿内容与连接关系。
- 大图预览缩放、适应、键盘关闭和焦点循环；实际加载图片后才能应用。
- 新增故障与完整交互回归，将产品验收加入 `npm run check`；补充不调用真实 AI 的 CI 工作流。
- 更新当前产品与技术规格、使用说明、贡献/安全/发布文档和分享包。

### 保留范围

本机网页、单画布、Codex/Qoder CLI、原生 `.drawio`、PNG/SVG/PDF 导出。没有新增服务器部署、多人协作、桌面安装包或多页支持。
