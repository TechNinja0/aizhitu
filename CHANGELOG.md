# 更新记录

## 1.2.1 · 2026-09-20

- 文件库新增独立创建人列，与最后保存人区分；创建人持久保存，旧文件按创建版本或创建者身份补全。

- 修复分享链接点击反馈不明显：立即显示分享窗口，复制失败可手动选中链接；剪贴板 API 被拒绝时尝试备用复制，分享不再等待文档同步。

- 工作区入口简化为首次填写用户名，同一浏览器自动记住身份；移除密码、访问码和退出入口，支持修改用户名而保留文件归属。
- 同名不会接管他人的身份；断网保留浏览器凭据，有效旧会话自动迁移。管理员入口限定服务电脑本机地址，旧远程管理员转为成员。
- 保留单人编辑锁、保存后自动同步、版本和回收站；补充身份持久化、旧会话迁移和多人浏览器回归验证。

## 1.2.0 · 2026-09-18

- Node.js 最低版本调整为 22.22，推荐 24 LTS，以匹配内置 SQLite 的已验证运行环境。
- 新增局域网共享文件库、个人账号与成员/管理员角色、独立文档 URL、重命名、搜索、回收站和分享链接。
- SQLite 持久保存文档和最近 50 个服务器版本；编辑锁绑定账号和标签页，30 秒租约、2 秒续约/自动保存/同步，版本冲突拒绝覆盖。
- 断线或锁失效后自动只读并保留本地修改，支持下载恢复副本、重新获取编辑权和加载最新版本。
- AI 任务按账号/文档归属，串行排队并可取消；仅管理员配置服务器客户端，导出结果按账号隔离。
- 新增多人 API 竞争测试与多浏览器模拟，补齐部署、备份、迁移和使用说明。保留 `/local` 临时画布。

## 1.1.0 · 2026-09-18

### 修复

- 画布改为普通滚轮滚动、Command+滚轮缩放；空格+鼠标左键拖动平移，不触发页面滚动，输入文字不受影响。

- AI 客户端设置采用固定底栏，保存按钮突出显示并标明未保存状态；连接测试单独分区，小窗口滚动时保存仍可见。

- 候选预览点击后立即打开独立大图窗口，展示加载、失败、重试状态；不再把结果藏在差异列表下方。
- 统一规范化后的内容摘要，忽略属性顺序、等价数值写法和样式顺序；解决字体规范化引发的候选误过期。
- 原稿编辑后撤销回相同内容可应用候选；使用新快照 revision 防止比较后发生的新编辑被覆盖。
- 候选可单独下载；预览失败或真实内容冲突不会迫使用户丢弃 AI 结果。

### 新增与完善

- 10 套主题、填充/边框/文字自定义颜色、十六进制输入、最近 10 个颜色快捷按钮。
- 复制/粘贴外观、6 种对齐和 2 种等间距排列；跳过锁定对象，保持图稿内容与连接关系。
- 大图预览缩放、适应、键盘关闭和焦点循环；实际加载图片后才能应用。
- 新增故障与完整交互回归，将产品验收加入 `npm run check`；补充不调用真实 AI 的 CI 工作流。
- 更新当前产品与技术规格、使用说明、贡献/安全/发布文档和分享包。

### 保留范围

本机网页、单画布、Codex/Qoder CLI、原生 `.drawio`、PNG/SVG/PDF 导出。没有新增服务器部署、多人协作、桌面安装包或多页支持。
