# 安全说明

AI智图是本地运行的绘图工作台。服务默认仅监听 `127.0.0.1`，v1.6.0 可显式开启可信局域网共享，提供固定账号、会话与编辑锁；不提供公网托管或租户隔离。可通过证书参数启用工作台与画布 HTTPS；未配置时 HTTP 传输仍未加密。

## 使用边界

- 安装依赖和 Chromium 需要联网；本地编辑、校验和导出不要求连接绘图云服务。
- 图片、图稿被用户交给外部 AI 客户端后，其传输、存储和处理遵循相应客户端设置。
- 文档校验与资源限制不能替代对输入来源的判断；保持依赖和浏览器更新。
- 共享文档持久保存在 SQLite，完整备份需停止服务后复制整个数据目录；`.drawio` 可另作独立备份。浏览器草稿可能随站点存储清理丢失。
- 分享含嵌入源文件的 PNG / SVG 会同时分享源图稿内容。

## 共享身份与数据

- 固定账号使用唯一登录名与密码；密码为随机盐 scrypt 哈希。显示姓名可以重复，不作为认领历史身份的凭据。新会话有效期为 30 天（记住登录）或 1 天，浏览器分别使用 localStorage / sessionStorage，数据库仅保存随机 token 的 SHA-256 摘要。
- 旧身份在原浏览器凭证验证后补设账号，保留 ID；没有旧凭证时由本机管理员核实后签发密码设置凭证。一次性凭证仅保存摘要，15 分钟过期，成功使用一次后失效。重置撤销原密码和全部旧会话，停用即时拒绝后续 API 并撤销编辑锁。已过期会话不会在重启后延长或复活。
- 管理员入口仅向服务电脑通过本机地址发起的请求提供，局域网请求不包含管理员凭据。本机入口仍然自动授予管理员身份，不要求账号密码；可操作服务电脑的人能够管理所有文件和用户。旧有效会话保留身份并撤销远程管理员角色；旧密码和访问码不再读取。
- 新稿未修改前只在当前标签页临时保留，首次修改或主动保存后才创建服务器文件；旧服务器草稿已合并到全部文件。保存后的文件默认私有；创建者或管理员可明确授权指定账号或所有成员，直接链接和历史版本接口也受相同权限校验。列表重命名、批量移入回收站和恢复仅创建者或管理员，服务器 AI 配置仅管理员。批量管理在单一事务校验权限、最新版本和不存在有效编辑锁，任一失败则全部回滚。
- 指定成员权限绑定随机身份 ID，不按用户名匹配；用户可在文件库核对完整身份编号。注册账号不提供实名认证，但同一账号可跨浏览器登录并保留权限。所有成员范围包含未来新加入的身份，本机管理员始终可管理全部文档。
- 收回权限立即阻止后续请求并移除失去访问权的编辑租约；在线页面轮询后移除画布。已下载、截图或浏览器缓存的内容无法远程收回。旧文件首次升级到 v1.4 默认私有，避免继续沿用旧版全员可见行为。
- 编辑租约绑定浏览器身份和标签页，保存必须同时匹配租约与服务器版本，不接受客户端自行强制覆盖。
- AI 任务按账号隔离，导出结果同样按身份校验。AI 使用服务器配置的 CLI 与额度。
- 数据目录包含浏览器身份会话，升级前的历史账号和加入码表保留但不再使用，备份不能作为分享安装包。默认私有目录权限和数据库文件权限限制其他系统用户读取。

## 本地 AI 和候选边界

- 本地 API 校验会话 token、Host 和 Origin；图稿编辑器独立 origin，不直接获得 AI 账号。
- CLI 路径使用固定参数数组与 `shell:false`。Qoder 的绘图调用禁用 Hook、内置工具和外部 MCP；Codex 使用 read-only。这些设置不修改全局客户端配置。
- 模型响应需经过格式、安全内容、对象关系及修改范围校验。运行面板仅显示受控事件摘要，不暴露原始日志、工具参数或隐藏推理。
- 应用候选前先完成图片预览，对当前内容和文档身份再次检查；在本地备份后由编辑器校验 revision，再一次性应用。AI 原稿变化时需再次明确确认覆盖并备份；共享文件的编辑租约和服务器版本校验不能通过候选覆盖跳过。
- 最近颜色保存在浏览器 localStorage；浏览器恢复草稿及本地版本在 IndexedDB；已保存的服务器文件在 SQLite，尚未保存的新稿初始内容及待确认的首次提交在 sessionStorage。候选任务在本地服务内存中有时限保存，下载源文件才是长期备份。

## 报告问题

如果仓库 GitHub 页面已开启 **Security → Report a vulnerability**，请优先私下报告。

若该入口尚未开放，请先提交不含漏洞细节的 Issue，请维护者开启私密报告渠道；在建立渠道前不要公开利用代码、敏感文件、令牌或真实业务图稿。

报告可包括影响版本、问题类型、最小复现条件、预期影响和修复建议。请使用合成数据复现。

目前是社区维护项目，没有固定响应时限或旧版本安全支持承诺。

用户删除采用保留身份记录的方式，需先停用且名下无图稿与个人模板。转移图稿保留原创建人和历史，收回原分享授权。账号管理审计不记录密码或一次性凭证。具体边界与 HTTPS 配置见 [账号指南](docs/ACCOUNTS.md)。
